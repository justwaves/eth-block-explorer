import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div``;

const BlockInfoPresenter = () => (
  <Wrapper>
    <>BlockInfoPresenter</>
  </Wrapper>
);

export default BlockInfoPresenter;
