import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div``;

const TransactionInfoPresenter = () => (
  <Wrapper>
    <>TransactionInfoPresenter</>
  </Wrapper>
);

export default TransactionInfoPresenter;
